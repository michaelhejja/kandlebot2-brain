# Only large binary model artifacts go through Git LFS.
# Source code, docs, notebooks, and config stay as plain git objects
# so they are readable without git-lfs installed.
*.pkl filter=lfs diff=lfs merge=lfs -text
*.joblib filter=lfs diff=lfs merge=lfs -text
